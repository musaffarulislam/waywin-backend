export interface ITrainerProfile {
    services: string[];
    description: string;
    tags: string[];
    experience: number;
    mode: string[];
    colorPalette: string;
}

export interface ITagArray{
    tags: string[];
}